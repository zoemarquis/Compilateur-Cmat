int main() {
  matrix a[3][2] = {{1, 2}, {3, 4}, {5.5, 6.6}};
  printmat(a);
  printf("\n");

  a++;
  printmat(a);
  printf("\n");

  ++a;
  printmat(a);
  printf("\n");

  a--;
  printmat(a);
  printf("\n");

  --a;
  printmat(a);
  printf("\n");

  return 0;
}
/* OK : correspond toujours à matrice - 1 (pas 1 - matrice) */